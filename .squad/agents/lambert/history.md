# Lambert History

## Core Context
- Project: forge, a Python regulatory impact framework for assessing regulatory change impact and scoring compliance using synthetic digital twin data.
- User: briandenicola.
- Current focus: maintain Microsoft Agent Framework / Foundry Hosted Agent readiness while implementing deterministic offline interpreter core only.

## Learnings

### 2026-07-21 (late evening, hardening) — Team update: MERGE branch is now schema-tolerant on column CASE (Fabric-notebook PascalCase compat)
- Bishop shipped a same-session hardening on top of `57daf7a` (the 17:05:16Z MERGE landing) — `write_delta_table` in `src/regimpact/lakehouse.py` now tolerates target tables whose columns are PascalCase (Fabric-notebook / Spark `saveAsTable` convention: `ID`, `Name`, `As_Of`, `Value_Chain`, ...) instead of hard-failing with `Schema error: No field named target.id`. Triggering failure was a real Fabric run against `business_processes`. Governing decision now at the top of `.squad/decisions.md`.
- **Nature of change:** boundary-layer accommodation for tables created OUTSIDE our writer (specifically by `01_load_lakehouse.ipynb`) — NOT a redesign of MERGE. Same file, same public API, same auth surface, same failure-class taxonomy per §0. User-approved MERGE rules 1–6 from `57daf7a` are preserved verbatim.
- **Direct impact on your work:** `export_regimpact_tables(...)` and `write_delta_table(...)` public signatures are **unchanged** — call-compatible with `57daf7a`. All callers (only in-repo caller is `cli.py::interpret`) are **untouched**. The clean writeback contract you established in the FabricMaterializer drop still holds: "Python owns Parquet-on-disk + Parquet+CSV in Files/ + latest-state Delta in Tables/, Fabric owns views on top", now sharpened to "...even when Fabric owns table creation and picks its own column casing". Failure semantics per decisions.md §0 unchanged.
- **Internal contract preserved:** `_TABLE_PRIMARY_KEYS` stays **lowercase-authoritative** — single source of truth for identity, no case duplication anywhere in the module. Column-case bridging happens at ONE place (a helper called `_align_arrow_to_target_schema`) on the MERGE branch only. Every non-MERGE codepath stays lowercase. If you touch table registration for any new Parquet output from `export_tables()` / `export_gold()`, keep the PK entry lowercase — the alignment helper handles the write-side case bridging.
- **Structural drift still refused loudly.** Case-drift is accommodation; missing columns are a hard error. Two new `LakehouseWriteError` failure modes carry the table name and the sorted target column list: (a) source column not present in target, (b) primary-key column not present in target. Both raise BEFORE any SQL is constructed — no silent data-shape corruption, ever. Same discipline as the extension-allowlist regression guard.
- **Design invariant to preserve on any future MERGE work:** the accommodation surface is column-CASE only. No collation tolerance, no type coercion, no autofix upstream. If you ever need to broaden it (unlikely — Fabric column naming doesn't drift on any other axis in practice), open a new decision entry rather than widening the helper silently.
- **Tests:** `tests/test_lakehouse.py` **46/46 green** (39 pre-existing + 6 new + 1 helper-driven update).

### 2026-07-21 (late evening, follow-up) — Team update: Delta writeback semantics flipped from append to MERGE upsert (boundary contract preserved)
- Bishop shipped a same-session follow-up to the 16:17:12Z Delta-append drop — `write_delta_table` in `src/regimpact/lakehouse.py` now MERGE-upserts on a per-table primary key instead of blind-appending. Every `interpret` run had been duplicating every row of every one of the 34 tables; now it dedups against the existing Delta log and only writes changed rows. Governing decision now at the top of `.squad/decisions.md`.
- **Nature of change:** semantic evolution of the just-shipped Delta layer — NOT a new feature, NOT a bug fix in the trilogy sense. Same file, same public API, same auth surface, same failure-class taxonomy per §0.
- **Direct impact on your work:** `export_regimpact_tables(...)` and `write_delta_table(...)` public signatures are **unchanged** — call-compatible with what shipped at 16:17:12Z. All callers (only in-repo caller is `cli.py::interpret`) are **untouched**. The clean writeback contract you established in the FabricMaterializer drop still holds: "Python owns Parquet-on-disk + Parquet-in-Files/, Fabric owns Delta + views", now sharpened to "Python owns Parquet-on-disk + Parquet+CSV in Files/ + latest-state Delta in Tables/, Fabric owns views on top". Failure semantics per decisions.md §0 unchanged.
- **Behavioural consequence you should know about:** Tables now hold **one row per primary key** (latest observed state), NOT one row per `(PK, as_of)` snapshot. Deliberate departure from the append behaviour — downstream Fabric SQL views already expected latest state; the append was accidentally producing garbage duplicates no consumer wanted. If SCD Type 2 history is ever needed, it would be a **separate `<table>_history` design**, not a revert.
- **Design invariant to preserve on any future writeback work:** the update predicate deliberately excludes `as_of`. Every row has a fresh `as_of` per run, so a naïve predicate including it would always evaluate "differs" and defeat the whole exercise. Encoded as user-approved rules 1–6 in the decision entry — if you touch the MERGE path, preserve those rules verbatim or open a new decision.
- **`_TABLE_PRIMARY_KEYS` map is now the source of truth for table identity semantics.** All 34 tables enumerated (28 single-`id`, 3 single non-`id` — `fact_gap`/`fact_remediation`/`fact_compliance_score`, 3 composite — `compliance_scores`/`relationships`/`bridge_gap_entity`). Unknown table = `LakehouseWriteError` (loud failure, not silent `("id",)` default). Any new Parquet output from `export_tables()` / `export_gold()` will need a PK entry added to that map or the Delta write will refuse it — treat this as a schema-boundary check, same discipline as the extension-allowlist regression guard.
- **Tests:** `tests/test_lakehouse.py` **41/41 green** (34 pre-existing + 7 new MERGE-path tests).

### 2026-07-21 (evening) — Team update: OneLake writeback now uploads CSV alongside Parquet (boundary contract extends without breaking)
- Bishop shipped a small additive extension to the OneLake writeback boundary — on top of the FriendlyNameSupportDisabled trilogy you saw close this afternoon. `export_to_lakehouse()` now uploads both `*.parquet` **and** `*.csv` from `tables_dir`; presence check in `export_regimpact_lakehouse()` widened to gate on `.parquet OR .csv`. Governing decision now at the top of `.squad/decisions.md`.
- **Nature of change:** NOT a fourth bug fix in the trilogy — the boundary is working end-to-end. This is a parity extension so users see the same file set in the lakehouse that they see on disk (`export_tables()` + `export_gold()` already write both formats locally).
- **Direct impact on your work:** `export_regimpact_lakehouse` public signature is **unchanged**. Return shape (`list[str]` of ABFSS URLs) is unchanged, just longer. Auth path unchanged. Failure semantics per decisions.md §0 unchanged. The clean writeback contract you established in the FabricMaterializer drop ("Python owns Parquet-on-disk + Parquet-in-Files/, Fabric owns Delta + views") **extends cleanly** — now it's "Parquet + CSV in Files/", and the notebook still owns Delta + views on top of the Parquet.
- **Deliberate constraint you should know about:** the glob is restricted to `parquet + csv` — NOT `*`. `output/tables/` is a working directory and may accumulate editor swap files / stray `.json`/`.txt`/`.md` scratch / half-written debug dumps. Widening to `*` would push those into the Fabric lakehouse and risk name collisions with legitimate assets. Regression guard `test_export_to_lakehouse_ignores_other_extensions` enforces this — anyone tempted to "just glob everything" in a future refactor will trip that test and land on the decision entry.
- **Notebook (`01_load_lakehouse.ipynb`) unaffected** — it reads by exact `{name}.parquet` filename, so CSVs sitting alongside are inert to the Spark loader. Parquet remains the primary format for Delta materialization; CSVs are for direct download / Excel / non-Spark consumers.
- **Fabric-side cost:** object count roughly doubles per subpath (17 → ~34), storage cost negligible at these row counts, wall-clock upload time stays sub-second at test scales.
- **Tests:** `tests/test_lakehouse.py` **23/23 green** (17 pre-existing + 4 new CSV-specific + 2 pre-existing parametrized cases).

### 2026-07-21 (afternoon) — Team update: OneLake writeback boundary is now definitively working end-to-end
- Bishop landed the third and final fix in the FriendlyNameSupportDisabled trilogy this afternoon — dropped the `.Lakehouse` suffix from ADLS Gen2 paths in `src/regimpact/lakehouse.py::export_to_lakehouse`. Canonical form per Fabric REST (`GET /v1/workspaces/{ws}/lakehouses/{lh}` → `properties.oneLakeFilesPath`) is the bare lakehouse GUID; `.Lakehouse` is a Fabric UI / Spark-shortcut convention that the ADLS Gen2 name-plane rejects. User confirmed end-to-end: Parquet uploads succeed to `Files/regimpact_raw/` and `Files/regimpact_gold/`. Governing decision at the top of `.squad/decisions.md`.
- **Complete three-layer fix for the OneLake upload boundary is now in place:** (1) GUID validation via `_normalize_fabric_id` (2026-07-20); (2) regional endpoint override via `FABRIC_ONELAKE_DFS_ENDPOINT` + `_resolve_onelake_endpoint` (2026-07-21 am); (3) bare-GUID path (2026-07-21 pm). Same operator, same workspace, same server-side error string across all three — each fix unmasked the next layer.
- **Direct impact on your work:** your `export_regimpact_lakehouse` orchestrator has NOT changed — same public signature, same call sites, same behavior contract. Only the underlying path-building inside `export_to_lakehouse` has been corrected. The clean writeback contract you established in the 2026-07-21 FabricMaterializer drop ("Python owns Parquet-on-disk + Parquet-in-Files/, Fabric owns Delta + views") is preserved intact, and the writeback boundary you delegated to `lakehouse.py` + Bishop's validation is now definitively working end-to-end.
- **For any future OneLake work** (e.g., Delta materialization via `delta-rs` in Python, additional writeback targets, lineage emission), you can now confidently build on this boundary. The three-layer fix means the OneLake upload path is no longer a source of intermittent surprises — GUIDs are validated, regional routing is explicit, path prefix matches what Fabric REST advertises.
- **Diagnostic funnel for `FriendlyNameSupportDisabled` occurrences** (should you ever see this error again on Fabric integration work): validate GUID → validate/override endpoint → verify path prefix via Fabric REST metadata API. Captured in `.squad/skills/fabric-resource-id-validation/SKILL.md`.
- **Tests:** `tests/test_lakehouse.py` 19/19 green (17 existing + 2 new regression guards against `.Lakehouse` re-introduction).

### 2026-07-21 — Dropped FabricMaterializer + Livy layer; Python is now Parquet-only
- User (briandenicola) called the OneLake path over-engineered relative to the Fabric loader notebook (`src/regimpact/01_load_lakehouse.ipynb`) that already does Delta conversion + view creation natively inside Fabric with the provided `spark` session. Verdict: the Livy + Foundry-supervisor stack was duplicating work the notebook does — async batch polling, PySpark statement templating, and Foundry evidence wrapping added moving parts without changing the final Fabric state. Ripped it out. Reverses the Option-A decision in `.squad/decisions.md#2026-07-17-fabricmaterializeragent`.
- **New clean contract** (governed by `.squad/decisions/inbox/coordinator-drop-fabric-materializer.md`):
  - **Python owns:** Parquet-on-disk (`output/tables/`, `output/gold/`) + Parquet-in-Files/ (OneLake `Files/regimpact_raw/`, `Files/regimpact_gold/`). Full stop.
  - **Fabric owns:** Delta table materialization + `v_impact` / `v_compliance` / `v_capability_health` view creation. Driven by `01_load_lakehouse.ipynb` inside Fabric.
  - **OneLake surface area = `export_regimpact_lakehouse()` in `src/regimpact/lakehouse.py` + Bishop's GUID validation** (strip → `uuid.UUID()` → canonical-form check → map to `LakehouseNotConfiguredError`). That's now the ENTIRE writeback boundary. No Livy, no batch polling, no Foundry supervisor for materialization.
- **Deleted:** `src/regimpact/agents/fabric_materializer.py`, `src/regimpact/agents/fabric_materializer_spec.py`, `src/regimpact/agents/fabric_livy_client.py`, `tests/test_fabric_materializer.py`, `tests/test_fabric_livy_client.py`.
- **Edited:** `src/regimpact/agents/__init__.py` (removed `FabricMaterializerAgent` / `FabricMaterializerError` import + `__all__` entries); `src/regimpact/cli.py` (removed the materializer import block, the `if upload_succeeded:` materialize block, and the `upload_succeeded` flag itself — the OneLake success path just prints and moves on now).
- **Kept untouched:** `lakehouse.py` (Bishop's GUID validation lands as-is), `01_load_lakehouse.ipynb` (canonical Delta + views logic — target consumer of uploaded Parquet), `pipeline.py` (its `FABRIC_WORKSPACE_ID` ref is for executive_qa, not materializer), all Foundry/Fabric agents.
- **Constitution note:** the notebook is Fabric-native and is the agent's data-prep step, NOT a deterministic offline fallback for agent behavior. Rule #3 (no deterministic/offline fallback paths for agent behavior) is unaffected. The materializer removal actually *tightens* the boundary — we're no longer running deterministic ETL through a Foundry-wrapped Livy client just to produce `tool_evidence` uniformly.
- **CLI comment updated:** the notebook path reference in `cli.py` corrected from the stale `notebooks/08_fabric/01_load_lakehouse.ipynb` to the actual location `src/regimpact/01_load_lakehouse.ipynb`. (Unusual location, but per user: separate concern; don't rename.)
- **Tests:** `tests/test_lakehouse.py` 10/10 green (the touched boundary). Full suite: 103 passed / 23 pre-existing failures — all `agent_version="3"` env-drift class from Bishop's history + pytest-asyncio config gap; zero failures touch materializer/Livy code. `python -c "import regimpact; import regimpact.agents; import regimpact.cli; import regimpact.lakehouse"` clean.

### 2026-07-20 — Team update: OneLake env-var GUIDs validated at the `lakehouse.py` boundary (Livy follow-up flagged to you)
- Bishop hardened `src/regimpact/lakehouse.py::export_to_lakehouse` — both `workspace_id` and `lakehouse_id` are now stripped (whitespace → one layer of `'`/`"` quotes → whitespace) and validated via `uuid.UUID()` + canonical-form check at entry. Cleaned values thread into both the ADLS SDK calls AND the returned ABFSS URLs (matches the pattern you established in your OneLake writeback design). `Settings` is not mutated — validation is local to the function.
- **Trigger:** user hit `(FriendlyNameSupportDisabled) Request Failed with WorkspaceId and ArtifactId should be either valid Guids or valid Names` from Fabric ADLS when `FABRIC_WORKSPACE_ID` carried a copy/paste artefact. Same class of opaque server error you'd have hit if the env var had a trailing `\n` or surrounding shell quotes.
- **Failure-class mapping (aligned with your §0 semantics):** malformed / non-canonical / empty-after-strip now raise `LakehouseNotConfiguredError` (soft yellow skip), **not** `LakehouseWriteError`. Rationale: a malformed env var fails every run until fixed — that's config, not a transient upload issue. Keeps the `LakehouseWriteError` red channel for the transient / capacity / auth cases you designed it for.
- **`fabric_livy_client.py` is out of scope in this pass — flagged as follow-up for you.** If Livy also consumes `FABRIC_WORKSPACE_ID` or any Fabric GUID env var raw, apply the same pattern before Livy ships to production. The pattern is captured in `.squad/skills/fabric-resource-id-validation/SKILL.md` — strip → `uuid.UUID()` → canonical-form check → map to the "not configured" error class of the surrounding module.
- Signature-stable, no new dependencies (`uuid` is stdlib), no test scaffolding to unwind. Tests: `tests/test_lakehouse.py` 10/10 green.

### 2026-07-17 — Team update: GapAnalyst now accepts empty control_ids with reason
- Bishop (this session) extended the empty-with-reason contract to `GapAnalysisRequest`. Contract mirrors the `ControlMappingResponse` pattern: optional `reason: str | None` field, validator accepts empty `control_ids` iff `reason.strip()` is non-empty. `obligation_ids` remains unconditionally required (obligations are pipeline input, not a downstream artefact).
- Pipeline now forwards `cm_response.reason` into `GapAnalysisRequest(reason=...)` when mappings is empty, and emits a second WARNING (`Fabric stage propagating empty control_ids stage=gap_analyst`) so the propagation is visible in log tails.
- **If future Foundry prompt spec docs are written for `RegImpactGapAnalyst` (or any other downstream Fabric agent), use the same reason-required-when-empty language.** Same field name (`reason`), same `reason.strip()` validation shape — cross-pipeline consistency matters more than locally-clever names. The response contract also stays symmetric: only `tool_evidence` is required, `findings` may be empty when every obligation→control pair meets its target maturity.
- Shipped in `1df0f5c`. 62 tests pass, 6 deselected (pre-existing agent_version drift).

### 2026-07-17 — Foundry prompt spec + evidence-starvation triage
- Deliverable A: shipped `docs/foundry-agent-prompts/control-mapper.md` — the paste-ready Foundry portal prompt spec Hamza needs for `RegImpactControlMapper`. Covers the response contract (per-mapping + top-level + tool_evidence fields), two full JSON examples (populated mappings AND empty-with-reason), the paste-ready system-prompt directive block, the harness retry contract, numbered deployment steps, and the version-pin recommendation. Reconciled the v3/v4 drift in `docs/foundry-fabric-agents.md` with a note pointing at the new spec.
- Deliverable B (evidence starvation): investigated and confirmed **no code fix available within the excluded surfaces**. Wrote up in `.squad/decisions/inbox/lambert-evidence-and-prompt.md`. Root cause = deployed `RegImpactControlMapper` v4 self-reports a single stub `tool_evidence` entry for the whole batch. Fix is prompt-side (directive 3 in the spec).
- **Key architecture insight**: `tool_evidence` on `FabricQuestionResponse` is a **passthrough** from `foundry_client._fabric_response_from_payload` — it's not gathered by the harness. `lakehouse.py` is writeback-only, not a query path. `pipeline.py` already sends `candidate_controls` shortlist + per-obligation `candidate_control_ids` inline, so the agent isn't starved of *data*; it's starved of *reporting discipline*.
- **Guardrails observed**: did NOT touch `contracts.py`, `_validated` / `_ask` / `map_controls` in `fabric_workflow.py`, or the control_mapper stage in `pipeline.py`. Considered and rejected post-hoc synthesizing `tool_evidence` (Constitutional "no offline fallback" violation) and touching `CONTROL_MAPPER_SPEC.instructions` (would duplicate the prompt fix and invite drift with the portal).

### 2026-07-17 — Team update: semantic retry removed from Fabric client
- The semantic retry loop that wrapped `FabricDataAgentClient.ask` (commit `412d695`) has been removed for latency (commit `f3d6ab4`, hamza-dev). `ask` is now single-attempt.
- Transport-level retry inside `FoundryAgentClient._invoke_with_retry` is untouched — network / 429 / 5xx retries still work. The change only removed the *semantic* retry that was re-issuing calls when the parsed payload looked wrong.
- OneLake writeback path is unaffected — `export_to_lakehouse()` and the `[fabric]` extra pattern are unchanged.

### 2026-07-17 — OneLake writeback for `interpret`
- Built `src/regimpact/lakehouse.py` — uploads every `*.parquet` from `settings.tables_dir` into a Fabric lakehouse under `Files/tables/`. Wired into `interpret` only (not `demo`, `analyze`, `score`, `audit`) — that's phase 1 scope.
- **ABFSS URL pattern** for OneLake-hosted lakehouses:
  `abfss://{workspace_id}@onelake.dfs.fabric.microsoft.com/{lakehouse_id}.Lakehouse/Files/{subpath}/{filename}`
  The ADLS account is always `https://onelake.dfs.fabric.microsoft.com`; the *filesystem* is the workspace ID, and the top-level directory is `{lakehouse_id}.Lakehouse`. Files land under `Files/…`, Delta tables land under `Tables/…`.
- **Lazy-import pattern for optional cloud SDKs:** `from azure.storage.filedatalake import DataLakeServiceClient` lives INSIDE `export_to_lakehouse()`, not at module top. Same for `DefaultAzureCredential`. This lets `from regimpact.lakehouse import ...` succeed even without the `fabric` extra installed, and lets tests plant a fake `azure.storage.filedatalake` in `sys.modules` before the import runs. Added a `fabric` optional extra to `pyproject.toml` (kept separate from `foundry`).
- **Why upload is best-effort, not fatal:** local Parquet in `output/tables/` is the source of truth. The `interpret` command wraps the OneLake call in a try/except that catches `LakehouseNotConfiguredError` (skip silently with a yellow hint) and `LakehouseWriteError` (red warning, but DO NOT re-raise). Rationale: a Fabric outage, expired token, or capacity-off state should never break a local pipeline run — the user still has their Parquet on disk and can re-upload manually.
- **Constitution alignment:** this is I/O plumbing, not agent behavior, so the "no deterministic offline fallback for agent behavior" rule doesn't apply — best-effort writeback for a data-export path is fine.
- **Testing:** all 5 new tests in `tests/test_lakehouse.py` pass; `tests/test_export_audit.py` unchanged (4/4 pass). Pre-existing failures in `test_cli.py::test_ask_fabric_cli_surfaces_missing_configuration` and `test_interpret_cli_surfaces_missing_foundry_configuration` are environmental (real `.env` credentials override `Settings()` defaults) and predate this change.

## Team Updates

### 2026-07-17 — Fabric response layer hardened
2026-07-17 — Bishop hardened the Fabric Data Agent response layer. Envelope missing `citations`/`tool_evidence`/`confidence` now defaults with a warning instead of aborting. Semantic retry (3 attempts) sits above transport retry. Inner-payload recovery treats known inner-shape JSON as the answer when the envelope is missing. See decisions.md.

### 2026-07-17 — Truncated inner-answer JSON now retryable
2026-07-17 — Bishop extended the Fabric semantic-retry loop to catch truncated inner-answer JSON — a follow-on to §5. Truncation (unclosed brace/bracket at EOF) triggers a concise-mode retry prompt asking the model to shorten rationales, drop optional fields, and cap answer size. Prose-answer agents (executive_qa, score_narrator) are exempted from JSON validation. See decisions.md.


### 2026-07-17 — Team update: request/response diagnostics landed

- Coordinator split a mixed working tree into two commits after user approval.
- **`1df0f5c`** — the diagnostics work you flagged as needed for the empty-mappings failure mode SHIPPED:
  - Per-obligation `candidate_control_ids` inline in the request payload (was already there for the shortlist; now surfaced per-obligation too — makes prompt-side reasoning about which controls were considered per obligation legible).
  - INFO/WARNING evidence logging in `pipeline.py` around the control_mapper → gap_analyst boundary. New WARNING `Fabric stage propagating empty control_ids stage=gap_analyst change_id=... reason=...` fires when the empty-with-reason path activates.
  - `tool_evidence_count` surfaced per stage in the pipeline logs.
- Your version-pin recommendation (`FOUNDRY_CONTROL_MAPPER_AGENT_VERSION`) remains the primary durable remediation for the v4 evidence-collapse drift — recorded as a standalone decision in `.squad/decisions.md`. Portal-side prompt directive (one `tool_evidence` per obligation batch) is the paired fix and lives in your Deliverable A spec.
- **`6cc6ffd`** — new `FabricMaterializerAgent` (Option A: deterministic Livy, Foundry as boundary supervisor). Adds a 6th stage between OneLake upload and downstream Fabric queries. If you author the Materializer prompt spec next, note the boundary posture: LLM does no PySpark generation at runtime; the agent plans + witnesses code-versioned statements from `fabric_livy_client.py`.
- 76 passed / 7 deselected. Pre-existing v3/v4 drift tests still deselected — waiting on your portal version-pin to land in env.